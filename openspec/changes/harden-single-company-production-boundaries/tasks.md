## 0. 批准与基线

- [x] 0.1 评审并明确批准本提案；批准前不修改本提案涉及的生产契约。
- [x] 0.2 固化当前数据库备份恢复、IIS 路径、无限预览、刷新突发和单公司数据基线。

## 1. 部署与健康边界

- [x] 1.1 统一 IIS/Nginx 的单一 `/api` 前缀并增加登录、刷新、登出、静态回退验收。
- [x] 1.2 增加 live/ready/AI capability 分层健康检查和代理配置。
- [x] 1.3 增加恰好一个公司的启动不变量与故障诊断测试。

## 2. 数据库迁移边界

- [x] 2.1 为迁移增加安全/破坏性分类和待执行检查。
- [x] 2.2 提供要求已验证备份的显式破坏性迁移命令。
- [x] 2.3 覆盖首次部署、普通升级、被拒绝的破坏性升级、中断恢复与旧版本回退。

## 3. 文件与预览预算

- [x] 3.1 拒绝 `previewRows <= 0` 并统一行列窗口硬上限。
- [x] 3.2 为上传、解压后工作簿尺寸、工作表维度和并发解析增加预算。
- [x] 3.3 将文件对比改为临时文件/有界窗口处理，并验证大文件内存上限与取消清理。

## 4. 认证与数据生命周期

- [x] 4.1 将刷新限流改为会话/用户分区并覆盖共享出口 IP 启动突发。
- [x] 4.2 增加安全版本校验、角色/组织/账号变更失效和刷新会话撤销。
- [x] 4.3 限制审计与执行历史分页并增加刷新会话、审计和执行历史的分批保留清理。
- [x] 4.4 将种子账号、用户创建、密码重置及前端校验统一为 4 到 200 个字符。

## 5. 验证与发布

- [x] 5.1 运行定向后端、前端、迁移和代理验收测试。
- [x] 5.2 运行一次完整回归、生产构建、真实备份恢复和 OpenSpec 严格校验。
- [ ] 5.3 在受控内网环境完成分阶段发布与回滚演练。
