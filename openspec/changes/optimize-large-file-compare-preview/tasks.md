## 0. 批准

- [x] 0.1 评审并批准本提案；批准前不实施。

## 1. API 窗口与差异载荷

- [x] 1.1 为表格预览增加经过校验的行列窗口参数和总量元数据。
- [x] 1.2 增加 `includeUnchanged` 处理，同时保持完整统计和导出行为。
- [x] 1.3 增加上限、无效偏移、尾部窗口、结构化/文本对齐和仅差异载荷 API 测试。

## 2. 前端有界预览

- [x] 2.1 用有界行列窗口和范围控件替代 `previewRows=0`。
- [x] 2.2 只渲染当前窗口，并为左右两侧提供上一页、下一页和差异导航。
- [x] 2.3 为表格列表、预览和对比请求增加 AbortSignal 与请求版本保护。

## 3. 行为验证

- [x] 3.1 增加延迟响应测试，证明过期 Sheet/文件响应会被忽略。
- [x] 3.2 增加组件测试，证明大型逻辑表单次最多渲染配置窗口。
- [x] 3.3 增加快速切换 Sheet、分页、仅差异和取消的 Playwright 覆盖。
