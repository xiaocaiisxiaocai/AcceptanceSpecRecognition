## 0. 批准

- [x] 0.1 用户已确认仅学习已选 Sheet、全部完成后进入下一步，并明确要求实施。

## 1. 实施

- [x] 1.1 增加智能填充所选 Sheet 批量确认编排与失败定位。
- [x] 1.2 收集各 Sheet 最终草稿并移除卡片内重复确认按钮。
- [x] 1.3 将底部主操作串联校验、确认学习和进入匹配配置。
- [x] 1.4 让推荐且可处理的待确认 Sheet 默认勾选，保持可选、跳过和拒绝状态不勾选。

## 2. 验证

- [x] 2.1 覆盖已选/未选、自动采用、缺少草稿和中途失败场景。
- [x] 2.2 运行相关前端测试、类型检查与 OpenSpec 严格校验。
- [x] 2.3 覆盖推荐待确认、可选、拒绝和零置信度的默认选择边界。

## 3. 字段候选冲突确认

- [x] 3.0 用户已明确同意在同字段出现无法可靠区分的高置信候选时弹框选择。
- [x] 3.1 后端返回同字段高置信候选的列索引、表头、置信度与样例。
- [x] 3.2 文件级主操作在学习前统一拦截未解决的字段候选冲突。
- [x] 3.3 弹框展示候选列、数据范围与样例，并把用户选择写回最终确认草稿。
- [x] 3.4 覆盖无冲突直达、冲突选择、取消保留和多 Sheet 顺序处理场景。

## 4. 冲突确认前置

- [x] 4.0 用户已确认字段冲突应在结构候选识别后、正式匹配前处理，且数据导入和智能填充保持一致。
- [x] 4.1 识别完成后立即拦截默认已选 Sheet 的冲突，确认前不生成正式匹配配置。
- [x] 4.2 推荐候选默认预选，但只有用户点击确认后才生效；无推荐项时保持未选。
- [x] 4.3 保留后续勾选冲突 Sheet 的文件级兜底门禁。
- [x] 4.4 覆盖前置确认、取消停留、无冲突直达和后续勾选兜底场景。
