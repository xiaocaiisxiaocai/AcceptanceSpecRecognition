## 1. 规格与模型

- [x] 1.1 固定匹配引擎为统一多阶段流程，移除 `SingleStage` 策略及其前后端配置入口
- [x] 1.2 设计并落地 `MatchEvidence`、硬冲突、歧义状态、自动采用门禁与复核结果的数据结构
- [x] 1.3 明确可配置知识来源的边界，包括别名字典、单位换算、冲突词对、关键字段槽位与 Prompt 模板

## 2. 后端实现

- [x] 2.1 重构匹配主流程，使 Embedding 仅承担 TopK 召回职责
- [x] 2.2 实现数值约束、型号/料号、品牌/单位实体、方向词和布尔条件的证据提取能力
- [x] 2.3 实现硬冲突过滤、证据重排和自动采用门禁
- [x] 2.4 实现仅对高歧义样本触发的 LLM 复核流程，并在失败或超时时回退为人工确认
- [x] 2.5 更新预览、批量预览、执行与批量执行接口，返回并消费新的决策字段

## 3. 前端实现

- [x] 3.1 移除智能填充界面中的 `SingleStage / MultiStage` 策略切换，改为展示统一匹配配置与复核策略
- [x] 3.2 在预览列表与详情弹窗中展示关键证据、冲突、歧义与复核状态
- [x] 3.3 调整执行填充交互，确保无法自动采用的样本必须人工确认

## 4. 配置与数据

- [x] 4.1 为品牌/组织别名、单位换算、冲突词对、关键字段槽位与 Prompt 模板选择并接入可配置存储
- [x] 4.2 准备首批高频品牌、单位与冲突词配置基线，避免首版完全依赖空词典运行

## 5. 验证与发布

- [x] 5.1 补充核心单元测试，覆盖数值相容、数值冲突、型号冲突、品牌别名、方向词冲突与复核失败场景
- [x] 5.2 补充 API 与前端联调测试，验证预览、复核、人工确认与执行填充的一致性
- [x] 5.3 建立一批真实业务样本作为回归基线，对比强改前后的误判与漏判变化
- [x] 5.4 更新用户说明与发布说明，明确 `SingleStage` 已移除且高歧义样本默认更保守
