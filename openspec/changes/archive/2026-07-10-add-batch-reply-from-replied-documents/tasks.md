## 1. 后端能力
- [x] 1.1 设计批量回复临时会话模型，支持来源文件、目标文件和多表格配置
- [x] 1.2 新增批量回复预检 API，按严格复用规则校验同格式目标文件
- [x] 1.3 新增批量回复执行与下载 API，只写回验收列和备注列
- [x] 1.4 复用现有文档读取、表格提取、写回与打包下载组件，避免复制逻辑

## 2. 前端与权限
- [x] 2.1 新增“批量回复”菜单、页面与独立权限码
- [x] 2.2 新增独立页面，支持上传已回复源文档、多表格配置和本地目标文件上传
- [x] 2.3 实现“预检 -> 执行 -> 下载”的两段式交互，并明确提示不调用 AI、不做模糊匹配

## 3. 验证
- [x] 3.1 补充批量回复 API、权限和页面回归测试
- [x] 3.2 运行 `openspec validate add-batch-reply-from-replied-documents --strict`
- [x] 3.3 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`
- [x] 3.4 运行 `pnpm build`
