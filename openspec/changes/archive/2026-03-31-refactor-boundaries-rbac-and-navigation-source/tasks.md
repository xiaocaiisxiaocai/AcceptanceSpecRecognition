## 1. 依赖边界与项目结构

- [x] 1.1 新增 `AcceptanceSpecSystem.Application` 项目并纳入解决方案
- [x] 1.2 调整项目引用，固定依赖方向为 `Api -> Application -> Core / Data`
- [x] 1.3 移出 `Data -> Core` 反向依赖与相关适配器、映射、枚举耦合
- [x] 1.4 为新的 Application 层补充基础 DI 注册与测试装配

## 2. 工作流拆分

- [x] 2.1 将文档导入流程从 `DocumentsController` 中拆出为独立 Application 用例服务
- [x] 2.2 将匹配预览、LLM 流式复核、执行填充、下载、严格复用、任务快照从 `MatchingWorkflowService` 拆为独立用例服务
- [x] 2.3 将共享的文件读取、表格提取、任务快照与结果写回逻辑下沉为小型协作组件
- [x] 2.4 将控制器收敛为薄协议层，只保留参数校验、调用和响应映射

## 3. 数据访问一致性

- [x] 3.1 清理控制器和协议适配层对 `AppDbContext` 的直接依赖
- [x] 3.2 在 Application 内统一写入路径与复杂查询路径
- [x] 3.3 统一 `Repository / UnitOfWork / Query` 的职责边界并补充约束测试

## 4. RBAC 与单组织契约收敛

- [x] 4.1 将系统用户、角色、组织、权限字典接口改为通过独立 Application 用例服务实现
- [x] 4.2 统一单角色、单根组织的 DTO、校验、种子初始化与数据范围计算
- [x] 4.3 清理多组织、多节点范围在 API 与 UI 上的无效能力面
- [x] 4.4 更新相关测试，明确新的单组织/单角色契约

## 5. 导航元数据与前端启动收敛

- [x] 5.1 新增共享导航元数据清单，作为页面/菜单/权限码的单一来源
- [x] 5.2 前端静态路由改为消费共享元数据，不再在多个模块重复维护权限码
- [x] 5.3 后端权限种子初始化改为消费同一份导航元数据
- [x] 5.4 前端启动流程移除对 async-routes 的运行时依赖，兼容接口仅保留空实现

## 6. 验证

- [x] 6.1 补充或更新架构约束测试，验证项目引用与协议层边界
- [x] 6.2 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`
- [x] 6.3 运行 `pnpm build`（目录 `web/`）
- [x] 6.4 冒烟验证导入、匹配预览、执行填充、RBAC 管理与导航权限过滤
