## 1. Implementation
- [x] 1.1 为 `AuthUserOrgUnits` 编写数据迁移，清洗历史多组织/无组织用户，并增加 `UserId` 唯一约束。
- [x] 1.2 调整系统用户 DTO、控制器与仓储查询，统一改为 `orgUnitId` 单值字段并移除主组织口径。
- [x] 1.3 调整认证访问服务与数据范围服务，改为基于唯一组织关系计算用户组织上下文。
- [x] 1.4 扩展权限类型与种子数据，补齐菜单权限码并更新角色权限同步逻辑。
- [x] 1.5 更新前端系统用户 API、用户缓存、系统用户管理页面、动态路由过滤与权限字典展示。
- [x] 1.6 补充后端与前端测试，覆盖迁移规则、单组织校验、菜单权限可见性和类型检查。
- [x] 1.7 运行 OpenSpec 严格校验、后端测试、前端类型检查与必要的路由/权限验证。
