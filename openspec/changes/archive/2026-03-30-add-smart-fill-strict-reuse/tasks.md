## 1. Implementation
- [x] 1.1 设计“当前填充结果 -> 临时复用会话”的后端数据结构，补齐严格模式所需的表格配置、行签名和写回值快照。
- [x] 1.2 新增严格模式复用预检接口，支持多文件上传后返回逐文件校验结果与失败原因。
- [x] 1.3 新增严格模式复用执行接口，仅对预检通过的文件批量写回，并返回单文件或压缩包下载结果。
- [x] 1.4 更新智能填充页面，在填充完成后增加“一次性严格复用”入口、目标文件上传、预检结果与执行结果界面。
- [x] 1.5 明确限制该能力不调用 AI、不重新匹配、不保存长期模板，并在前后端提示文案中体现。
- [x] 1.6 补充后端集成测试与前端类型校验，覆盖严格一致、结构不一致、行序不一致和批量下载结果。
- [x] 1.7 运行 `openspec validate add-smart-fill-strict-reuse --strict`、后端测试与前端构建/类型检查。
