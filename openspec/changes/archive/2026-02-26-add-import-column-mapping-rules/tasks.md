# 实施任务清单

## 1. Proposal 阶段（必须先完成并获批）
- [x] 1.1 明确规则作用域（全局 / 客户 / 制程）
- [x] 1.2 明确匹配方式（包含/等于/正则）与冲突优先级策略
- [x] 1.3 `openspec validate add-import-column-mapping-rules --strict` 通过

## 2. 后端实现
- [x] 2.1 数据模型：列映射规则实体 + EF Core Migration
- [x] 2.2 API：列映射规则 CRUD
- [x] 2.3 API：导入页获取“当前生效规则”（按作用域合并）
- [x] 2.4 测试：规则 CRUD 与合并策略

## 3. 前端实现
- [x] 3.1 新增页面：配置管理 → 列映射规则
- [x] 3.2 导入页：基于规则对每个表格自动预填列映射
- [x] 3.3 UX：展示“自动匹配结果/未匹配提示/一键重新应用规则”

## 4. 验证
- [x] 4.1 `pnpm build` 通过
- [x] 4.2 `dotnet build` 通过
