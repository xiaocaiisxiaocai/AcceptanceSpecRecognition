## ADDED Requirements

### Requirement: 列映射规则数据模型
系统 SHALL 提供用于存储列映射规则的数据模型，至少包含：目标字段、匹配模式、匹配词列表、优先级与作用域信息。

#### Scenario: 规则可被完整还原
- **WHEN** 系统保存列映射规则
- **THEN** 之后读取时应能还原出相同的规则配置（不丢失匹配模式、词条、优先级与作用域）
