## 1. Proposal Approval
- [x] 1.1 用户确认阶段 3 只做列语义召回，不做仅规格导入。
- [x] 1.2 用户确认第一版采用 LLM 裁决，不引入 Embedding 字段向量库。

## 2. Core Models
- [x] 2.1 定义列语义召回请求/响应模型，包含未映射表头、候选字段、置信度、理由和来源。
- [x] 2.2 扩展列识别明细，支持返回语义召回建议但不覆盖确定性映射结果。

## 3. LLM Service
- [x] 3.1 实现列语义裁决服务或复用现有结构裁决服务的子场景。
- [x] 3.2 Prompt 明确字段定义、禁止映射示例和 Unknown 输出规则。
- [x] 3.3 增加预算、超时、解析失败和非法字段校验。

## 4. Smart Structure Integration
- [x] 4.1 在规则列映射和健康检查后判断是否触发列语义召回。
- [x] 4.2 将语义召回建议合并到智能结构识别响应中。
- [x] 4.3 任一关键字段依赖语义召回时保持 `NeedConfirm`，不得自动采用。
- [x] 4.4 用户确认后继续走现有列映射学习机制。

## 5. Tests
- [x] 5.1 Core 测试：规则完整命中时不调用 LLM。
- [x] 5.2 Core/API 测试：未映射语义表头返回建议。
- [x] 5.3 Core/API 测试：方法列不误判为结果列。
- [x] 5.4 API 测试：AI 建议补齐后仍需人工确认。
- [x] 5.5 API 测试：LLM 失败或非法输出回退规则结果。

## 6. 2026-07-10 维护性收口
- [x] 6.1 合并方法列/结果信号判定，并补齐同列去重。
- [x] 6.2 新增列语义召回系统 Prompt 模板场景与映射。
- [x] 6.3 新增独立列召回超时配置并显式释放 LLM JSON 文档资源。
- [x] 6.4 增加 Core/API/Prompt 回归测试并完成验证。
