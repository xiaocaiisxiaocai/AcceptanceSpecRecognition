## 1. 步骤模型与导航
- [x] 1.1 为默认和高级流程补充具名步骤常量，将默认步骤调整为上传归属、识别确认、匹配配置、预览确认。
- [x] 1.2 识别成功后自动进入识别确认步骤，识别失败留在上传步骤并允许重试。
- [x] 1.3 更新下一步、上一步、预览步骤判断和请求取消逻辑，避免步骤下标错位。

## 2. 页面职责拆分
- [x] 2.1 上传步骤仅保留文件、归属、AI 辅助和识别状态，不再内嵌确认卡。
- [x] 2.2 新增独立识别确认步骤，展示紧凑来源摘要、识别汇总、Sheet Tab、问题和范围配置。
- [x] 2.3 让当前 Sheet 的确认/调整操作和阻断原因保持可见，并让底部按钮显示明确的剩余确认状态。
- [x] 2.4 保留手动高级配置入口以及文件、客户变化时的识别结果失效规则。

## 3. 回归验证
- [x] 3.1 补充步骤数组、识别成功跳转、失败留页、返回保留和预览步骤下标测试。
- [x] 3.2 补充待确认、全部自动采用、跳过 Sheet 和手动高级模式的页面守卫测试。
- [x] 3.3 运行前端 Vitest、Node 页面测试、类型检查、构建和代码规范检查。
- [x] 3.4 使用真实浏览器验证两 Sheet 待确认文件及全部自动采用文件的完整四步流程和响应式布局。
