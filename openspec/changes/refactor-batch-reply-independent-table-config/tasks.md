## 1. 后端契约与会话
- [x] 1.1 扩展批量回复 DTO，拆分来源逐表配置与目标逐表配置，并补充来源表选择字段
- [x] 1.2 重构批量回复会话模型，支持来源配置、目标文件配置和逐表预览上下文
- [x] 1.3 新增或调整预览接口，支持按目标文件/目标表返回逐行预览结果
- [x] 1.4 调整执行接口，按“单个目标文件配置完整”执行并返回逐文件结果

## 2. 核心校验与写回
- [x] 2.1 保持 `项目 + 规格` 键匹配，允许乱序但拒绝重复键
- [x] 2.2 让 Word 与 Excel 共用逐表列映射抽取逻辑
- [x] 2.3 执行前对目标文件做最小必要复检，但不再要求整批预检先通过
- [x] 2.4 保证写回仅更新验收/备注单元格，不删行、不删列、不裁剪表

## 3. 前端交互
- [x] 3.1 将批量回复页面重构为“来源配置 / 目标配置 / 执行结果”多 Tab
- [x] 3.2 来源配置页支持逐表独立预览与配置
- [x] 3.3 目标配置页支持逐文件、逐表独立配置，并允许选择来源表
- [x] 3.4 执行结果页展示逐文件、逐表结果和失败原因

## 4. 验证
- [x] 4.1 补充 API 回归测试，覆盖逐表预览、逐文件执行、乱序匹配和重复键拒绝
- [x] 4.2 补充前端/回归测试，覆盖多 Tab 文案与来源表选择
- [x] 4.3 运行 `openspec validate refactor-batch-reply-independent-table-config --strict`
- [x] 4.4 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`
- [x] 4.5 运行 `pnpm --dir web build`
