## 1. 后端组织树契约

- [x] 1.1 先补充组织树查询、新增、层级校验、下级编辑和删除保护的失败集成测试。
- [x] 1.2 在 `OrgUnitsController` 恢复新增与删除端点并增加审计标记。
- [x] 1.3 在 `OrgUnitAppService` 恢复完整树、平铺列表、节点创建、下级编辑、停用保护和引用保护删除。
- [x] 1.4 运行 `OrgUnitsTests` 并确认全部通过。

## 2. 用户归属与角色数据范围

- [x] 2.1 先将“非根组织应拒绝”的用户测试改为“有效下级组织可分配”，并观察失败。
- [x] 2.2 允许系统用户归属当前公司内任意有效组织节点，同时保持单组织唯一约束。
- [x] 2.3 先补充角色选择下级节点、组织子树和自定义节点集合的失败测试。
- [x] 2.4 放开 `AuthRoleAppService` 的根节点限制，校验全部范围节点属于当前公司且有效。
- [x] 2.5 恢复组织新增、删除 API 与按钮权限种子并更新权限测试。

## 3. 前端组织与角色管理

- [x] 3.1 先更新组织管理与角色范围的前端契约测试并观察失败。
- [x] 3.2 将组织管理页恢复为树形表格，支持新增下级、编辑、启停和安全删除。
- [x] 3.3 在新增弹窗中按父节点过滤合法下级类型，并允许向下跳级。
- [x] 3.4 在角色管理中恢复自定义节点范围，并根据范围类型切换单选或多选。
- [x] 3.5 验证系统用户页面能够展示并选择完整平铺组织列表。

## 4. 验证与运行

- [x] 4.1 运行受影响的 API、权限、用户、角色和数据范围测试。
- [x] 4.2 运行前端定向契约测试、类型检查、ESLint、Stylelint 和 Prettier。
- [x] 4.3 使用本地 MySQL 验证历史组织数据可读取且未被改写。
- [x] 4.4 重新启动当前 API，确认 5291/8849 健康并完成层级组织冒烟验证。
- [x] 4.5 复核差异、数据完整性、安全删除、审计和未提交文件边界。
