## 0. 批准

- [x] 0.1 评审并批准本提案；批准前不实施。

## 1. 仪表盘一致性

- [x] 1.1 为仪表盘汇总增加遵循数据范围、时区正确且补零的每日趋势。
- [x] 1.2 将首屏请求收敛为汇总加最近执行。
- [x] 1.3 增加 AbortController/请求版本保护，并分离周期刷新与手动完整刷新。
- [x] 1.4 增加 API 和延迟响应行为测试。

## 2. 包体积预算

- [x] 2.1 用可访问的原生 SVG 实现替换仪表盘 ECharts sparkline。
- [x] 2.2 移除未使用的 ECharts 代码，将 Element Plus 收敛为实际使用组件并增加完整性守卫。
- [x] 2.3 恢复有意义的 chunk 告警，并在 CI 增加阻断式 gzip 预算。

## 3. 键盘与语义导航

- [x] 3.1 将核心导航图标操作、Logo 和搜索结果改为语义交互元素。
- [x] 3.2 为页面标签实现 tablist 语义、游走焦点和键盘命令。
- [x] 3.3 增加可见焦点、无障碍名称、头像替代文本和 44px 交互目标。

## 4. 行为验证

- [x] 4.1 增加仪表盘竞态、SVG 状态和键盘交互的挂载组件测试。
- [x] 4.2 增加搜索、导航、设置和页面标签的纯键盘 Playwright 覆盖。
- [x] 4.3 执行生产构建并强制校验记录的包体积预算。
