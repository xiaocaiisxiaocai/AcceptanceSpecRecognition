## 1. Tests (RED)
- [ ] 1.1 增加固定 Word/Excel 快照的缓存命中与未命中结果一致性测试，覆盖元数据、结构化单元格、合并单元格、表头/数据坐标和行数上限
- [ ] 1.2 增加元数据、智能识别、预览和导入跨作用域复用同一内容版本解析结果的计数回归测试
- [x] 1.3 增加同键并发 single-flight、单个等待者取消、解析失败不缓存和后续可重试测试
- [ ] 1.4 增加文件哈希变化、文件删除/无权访问和主动失效测试，证明缓存不能绕过数据范围
- [ ] 1.5 增加独立容量、过期、超大条目不缓存和缓存禁用回退测试
- [ ] 1.6 增加匹配判断保护测试，证明 Prompt、AI 调用次数与输入、列映射、置信度和自动采用条件不因缓存命中改变

## 2. Implementation (GREEN)
- [x] 2.1 定义上传文档快照访问端口、内容版本键和调用方隔离的只读/深复制模型
- [ ] 2.2 实现带 single-flight 的独立有界快照缓存、大小估算、短时过期、取消与失败清理
- [x] 2.3 让 `DocumentTableAccessService` 的元数据、预览、匹配来源和导入读取复用快照投影
- [x] 2.4 让 `SmartConfigurationAppService` 通过同一快照端口读取文档，不改变后续规则与 AI 流程
- [ ] 2.5 在文件内容成功写回和删除生命周期中触发按 `FileId` 失效，并保持哈希版本兜底
- [ ] 2.6 增加脱敏的 cache hit/miss/shared/skip、解析、等待、投影和估算大小日志
- [x] 2.7 完成最小必要重构，移除本变更引入的重复解析入口且保持架构依赖方向

## 3. Verification
- [ ] 3.1 运行快照、文档访问、智能结构识别、预览和导入定向测试并记录 RED/GREEN 证据
- [ ] 3.2 运行受影响项目构建、架构测试和前端契约测试，确认 HTTP 与页面流程无需变更
- [ ] 3.3 使用同一固定 Excel 样本复测上传、元数据、识别、预览和导入，比较底层解析次数及各阶段耗时
- [ ] 3.4 对比缓存命中与禁用时的识别响应、导入请求和匹配采用结果，确认业务判断一致
- [ ] 3.5 执行只读代码评审，关闭缓存并发、权限、内存、取消和可变对象隔离的 Critical/Important 问题
