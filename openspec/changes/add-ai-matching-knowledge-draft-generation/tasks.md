## 1. 后端接口与草稿生成
- [ ] 1.1 定义 AI 草稿生成请求/响应 DTO，覆盖分类、输入来源、候选状态和导入模型
- [ ] 1.2 新增匹配知识 AI 草稿生成服务，负责文本收集、LLM 调用、结构化解析和冲突标记
- [ ] 1.3 在 `MatchingKnowledgeController` 增加草稿生成接口与权限校验
- [ ] 1.4 复用上传文档解析链路，支持已上传文档与临时上传文档取样

## 2. Prompt 与生成规则
- [ ] 2.1 为单分类草稿生成定义 Prompt 模板与结构化输出约束
- [ ] 2.2 约束“单位规则”只生成单位别名，不生成单位倍率
- [ ] 2.3 增加服务端去重、冲突检测和状态标记逻辑

## 3. 前端页面与审核弹窗
- [ ] 3.1 在匹配知识配置页各分类区域增加 `AI 生成候选` 入口
- [ ] 3.2 实现草稿弹窗，支持来源选择、文档上传/选择、生成、编辑、删除、勾选
- [ ] 3.3 实现草稿导入到自定义扩展，并按既定规则处理重复与冲突

## 4. 测试与回归
- [ ] 4.1 补充 API 测试，覆盖单分类生成、输入来源校验、冲突状态与不落库行为
- [ ] 4.2 补充前端回归测试，覆盖按钮入口、弹窗文案和导入交互
- [ ] 4.3 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`
- [ ] 4.4 运行 `pnpm build`
