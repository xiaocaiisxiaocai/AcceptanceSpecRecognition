## 1. 后端接口与草稿生成
- [x] 1.1 重写 AI 草稿生成请求/响应 DTO，仅保留分类、历史验规筛选条件、候选状态和导入模型
- [x] 1.2 为验规列表与筛选查询增加客户、制程、机型、关键词、导入时间范围支持，供弹窗预览复用
- [x] 1.3 重构匹配知识 AI 草稿生成服务，改为按历史验规筛选查询、拼接源文本、调用 LLM 和标记冲突状态
- [x] 1.4 删除粘贴文本、已上传文档、临时上传文档及文档解析相关后端分支/依赖

## 2. Prompt 与生成规则
- [x] 2.1 保持单分类草稿生成与结构化输出约束
- [x] 2.2 继续限制“单位规则”只生成单位别名，不生成单位倍率
- [x] 2.3 为筛选结果为空、输入超上限、重复与冲突状态补充服务端校验与提示

## 3. 前端页面与审核弹窗
- [x] 3.1 在匹配知识配置页各分类区域保留 `AI 生成候选` 入口，但弹窗只保留“历史验规”来源
- [x] 3.2 增加客户、制程、机型、关键词、导入时间范围筛选与命中结果预览
- [x] 3.3 实现“当前筛选结果默认全选，只支持全选/取消全选”的交互与生成提示
- [x] 3.4 保持草稿编辑、删除、导入到自定义扩展的现有审核流程

## 4. 测试与回归
- [x] 4.1 补充 API 测试，覆盖历史验规筛选生成、导入时间范围、空结果、超上限与不落库行为
- [x] 4.2 补充/更新前端回归验证，覆盖旧来源入口移除、筛选预览、全选语义与导入交互
- [x] 4.3 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`
- [x] 4.4 运行 `pnpm build`
