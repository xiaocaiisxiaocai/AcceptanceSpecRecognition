## 0. 批准

- [x] 0.1 评审并批准本提案；批准前不实施。

## 1. 运行时 AI 自动选择

- [x] 1.1 实现按用途隔离的 readiness 注册表、有界探测和缓存失效。
- [x] 1.2 增加只读自动选择端点和安全的组件级健康输出。
- [x] 1.3 更新人工连接测试与真实 AI 调用，以刷新 readiness 且不把瞬时失败持久化到 `IsDisabled`。
- [x] 1.4 增加优先级、用途隔离、缓存过期/失效、全部不可用和恢复 API 测试。

## 2. Keep-alive 业务控件

- [x] 2.1 在共享 AI 控件中以自动选择 API 替代配置列表选择。
- [x] 2.2 页面激活时刷新，并按权限展示 available、checking 和 unavailable 状态。
- [x] 2.3 确保没有 LLM 时规则识别仍可用，需要 Embedding 的流程继续明确阻断。
- [x] 2.4 数据导入与智能填充发起识别时有限等待短暂 checking 状态，并在恢复后采用最新 LLM 服务。

## 3. 上传进度与取消

- [x] 3.1 为共享上传 API 封装增加 AbortSignal 和上传进度选项。
- [x] 3.2 将 AppUploadZone 绑定真实请求 Promise，并展示上传/处理/成功/失败阶段。
- [x] 3.3 增加取消行为，清理失效状态且不把主动取消显示为失败。

## 4. 验证

- [x] 4.1 增加 keep-alive 刷新、上传进度、阶段变化和取消的组件测试。
- [x] 4.2 增加从 AI 配置返回以及慢速/取消上传的 Playwright 覆盖。
- [x] 4.3 增加 checking 恢复、确认不可用和流程取消不泄漏状态的共享识别回归测试。
