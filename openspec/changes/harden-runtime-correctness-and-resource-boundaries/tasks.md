## 1. 审计、错误响应与 API 正确性

- [x] 1.1 为最终响应状态、业务并发冲突和审计写入失败编写失败测试
- [x] 1.2 将审计上下文采集与最终状态持久化分离，并使用独立作用域
- [x] 1.3 为错误码建立真实 HTTP 状态映射并收敛未知异常响应
- [x] 1.4 运行审计和 API 错误边界定向测试

## 2. 前端异步流程与执行历史

- [x] 2.1 为执行历史分页、快速切换和完整逐行回放编写失败测试
- [x] 2.2 恢复智能填充完整回放并增加服务端分页、取消和请求代次
- [x] 2.3 为语义搜索作用域切换和结果编辑编写失败测试并修复
- [x] 2.4 为批量预览失效、执行/下载分离编写失败测试并修复
- [x] 2.5 为 SmartFill 失活取消和激活对账编写失败测试并修复
- [x] 2.6 将上传区删除动作改为“移出当前流程”，确认不调用持久文件删除 API

## 3. 后端并发、文件与安全边界

- [x] 3.1 为 Embedding 同键并发插入编写失败测试并实现唯一冲突恢复
- [x] 3.2 为持久文件删除状态机编写迁移与失败测试
- [x] 3.3 实现 `PendingDeletion` 幂等清理、重试和运维状态
- [x] 3.4 为重复分析候选预算、比较预算和取消编写失败测试并实现分桶比较
- [x] 3.5 为文件比较流式上传、扫描/差异预算和临时文件清理编写失败测试并实现
- [x] 3.6 为精确 Origin、Host 覆盖、代理、自动重定向和正常 TLS 编写行为测试
- [x] 3.7 实现统一 AI 受限客户端并接入探测与实际模型传输
- [x] 3.8 向 CRUD、批量删除和高成本循环传播取消令牌，收敛已知冲突映射

## 4. 依赖、测试与规范治理

- [x] 4.1 升级 `postcss` 和 `brace-expansion` 并更新前端锁文件
- [x] 4.2 固定 NuGet 版本及解决方案内 7 个项目的锁文件、GitHub Actions SHA 和可配置 npm registry；解决方案外 4 个工具项目不启用锁文件或 CI locked restore
- [x] 4.3 统一密码 4～200 规则并更新部署样例与文档
- [x] 4.4 删除无效 `ElResult` 注册，将脆弱源码正则测试改为行为/契约测试
- [x] 4.5 仅拆分本次修改直接涉及且职责混杂的组件或服务

## 5. 验证与交付

- [ ] 5.1 运行各切片定向测试并保存 RED/GREEN 证据
- [x] 5.2 在真实 MySQL 测试环境验证并发、迁移、清理重试和回滚
- [x] 5.3 运行执行历史、语义搜索、批量回复和 SmartFill 定向浏览器验收
- [x] 5.4 运行 .NET 全量测试、严格构建和 NuGet 漏洞检查
- [x] 5.5 运行前端测试、类型、Lint、格式、样式、构建、包体积和 npm audit
- [x] 5.6 运行全量 OpenSpec 严格校验和仓库卫生检查
- [x] 5.7 运行生产式 Docker 健康检查、迁移与回滚演练
- [ ] 5.8 仅在真实目标环境完成后更新既有受控发布任务；未经授权不推送、合并或部署
