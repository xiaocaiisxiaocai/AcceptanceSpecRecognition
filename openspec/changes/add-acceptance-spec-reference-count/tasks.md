## 1. 数据模型与迁移

- [x] 1.1 为验收规格实体增加引用次数字段和默认值。
- [x] 1.2 生成可回滚的 EF Core Migration，并登记为安全迁移。
- [x] 1.3 同步模型快照并核对本地数据库已应用最新迁移。

## 2. 引用累计与版本重置

- [x] 2.1 在 Excel 与 Word 最终提交事务内按采用行原子累计引用次数。
- [x] 2.2 保持同一执行请求重放幂等，并覆盖并发累计测试。
- [x] 2.3 对手工编辑、文档导入、回填和备注批量替换统一应用内容变更重置策略。
- [x] 2.4 对验收与备注均为空的规格保持不计数。

## 3. API 与前端展示

- [x] 3.1 在验收规格列表、详情和创建响应中返回引用次数。
- [x] 3.2 在验收规格列表和详情界面显示引用次数。

## 4. 验证

- [x] 4.1 运行引用次数 API 集成测试和迁移元数据测试。
- [x] 4.2 运行完整后端测试并与 `origin/main` 失败集合对比，确认功能分支没有新增失败。
- [x] 4.3 运行前端 Vitest、Node 测试、类型检查、代码规范检查和生产构建，并记录既有失败基线。
- [x] 4.4 运行引用次数前端定向测试、OpenSpec 严格校验和 Git 差异检查。
