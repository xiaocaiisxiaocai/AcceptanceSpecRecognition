## 1. Implementation
- [ ] 1.1 为 `AuthUserRoles` 编写数据迁移，清洗历史多角色/无角色用户，并增加 `UserId` 唯一约束。
- [ ] 1.2 调整系统用户 DTO、控制器与仓储查询，统一改为 `roleCode` 单值字段。
- [ ] 1.3 调整认证访问服务、登录/刷新接口与 JWT 生成逻辑，移除角色数组暴露。
- [ ] 1.4 更新前端系统用户 API、登录用户 API、用户缓存与系统用户管理页面，改为单角色展示和单选提交。
- [ ] 1.5 补充后端测试，覆盖迁移规则、单角色校验、最后一个 `admin` 保护和登录返回结构。
- [ ] 1.6 运行 OpenSpec 严格校验、后端测试与前端类型检查。
