## 1. 数据模型与迁移

- [x] 1.1 增加规格 Active/Quarantined 状态、隔离元数据和忽略后续扫描元数据。
- [x] 1.2 增加扫描任务、扫描结果和无正文永久删除留痕实体及查询索引。
- [x] 1.3 生成可回滚 EF Core Migration，将现有规格安全初始化为 Active，并验证模型快照。
- [x] 1.4 为扫描任务结果保留期和隔离期配置增加校验，禁止零天隔离。

## 2. 扫描判定与任务

- [x] 2.1 实现纯扫描分类策略，覆盖从未引用、长期未引用、保护期、旧版本近期引用和迁移前未知时间。
- [x] 2.2 实现有界批次扫描任务，持久化真实进度、分类计数、原因码和判定快照。
- [x] 2.3 实现任务取消、失败恢复和过期扫描结果清理，不影响规格数据。
- [x] 2.4 将扫描聚合和分页下推数据库，验证大数据量下无全表内存加载和 N+1 查询。

## 3. 隔离生命周期

- [x] 3.1 实现保留、忽略后续扫描和内容变化自动解除忽略。
- [x] 3.2 实现逐项结果的批量隔离，校验数据范围、状态、引用版本和扫描快照。
- [x] 3.3 实现隔离列表、恢复和 Embedding 缓存失效/重建行为。
- [x] 3.4 实现到期后永久删除、二次确认、依赖清理和无正文删除留痕。
- [x] 3.5 覆盖批量部分成功、并发内容/引用变化、未到期、越权和重复动作测试。

## 4. 消费者与架构边界

- [x] 4.1 建立共享 Active 规格查询规则，供仓储与应用查询复用。
- [x] 4.2 让列表/详情、重复排查、语义搜索、智能填充、严格复用、导入冲突和缓存任务排除隔离规格。
- [x] 4.3 新建清理应用服务并保持控制器只做协议适配，补充架构边界测试。
- [x] 4.4 检查现有直接删除接口兼容性，防止绕过隔离期永久删除已隔离规格。

## 5. API、权限与审计

- [x] 5.1 增加扫描启动/取消、状态、结果分页、忽略、隔离、恢复和永久删除 API 契约。
- [x] 5.2 在共享权限清单登记查看、隔离/忽略、恢复和永久删除权限及页面按钮映射。
- [x] 5.3 对所有查询和动作应用公司/组织数据范围，并覆盖不泄露越权数据测试。
- [x] 5.4 为隔离、恢复、忽略和永久删除写入脱敏审计，记录批量成功与失败摘要。

## 6. 前端交互

- [x] 6.1 增加扫毒清理 API 类型、请求封装和页面路由/入口。
- [x] 6.2 实现阈值设置、真实扫描进度、分类统计、分段视图和服务端分页表格。
- [x] 6.3 实现默认不预选的逐项/批量保留、忽略和移入隔离区交互及部分成功反馈。
- [x] 6.4 实现隔离区恢复、到期倒计时和永久删除二次确认，并按权限控制按钮。
- [x] 6.5 使用现有视觉令牌完成紧凑杀毒扫描效果，覆盖加载、空、失败、取消和权限不足状态。

## 7. 验证

- [x] 7.1 运行扫描分类、迁移、API 权限/数据范围、隔离状态机和消费者过滤定向测试。
- [x] 7.2 运行前端定向测试、类型检查和受影响模块构建。
- [x] 7.3 在真实浏览器验证扫描进度、四类视图、批量部分成功、隔离恢复和控制台/请求错误。
- [x] 7.4 运行 `openspec validate add-acceptance-spec-cleanup-scanner --strict` 并检查 Git 差异不包含用户未跟踪文件。
