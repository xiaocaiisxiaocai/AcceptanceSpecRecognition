## 1. 交互与回归

- [x] 1.1 为权限类型切换、资源分组、搜索过滤、仅看已选和分组选择建立前端回归测试。
- [x] 1.2 提取无副作用的权限分组与过滤逻辑，保持 `permissionCodes` 作为唯一提交值。

## 2. 角色权限选择器

- [x] 2.1 将现有长树改为权限类型切换与资源分组面板，并展示各类型和各资源的已选数量。
- [x] 2.2 增加关键词搜索、仅看已选、分组全选/清空，并确保筛选不会丢失已选权限。
- [x] 2.3 优化按钮与接口权限的主次文案、密度、滚动区域和只读状态。

## 3. 验证

- [x] 3.1 运行角色权限选择器定向测试与前端类型检查。
- [x] 3.2 在真实浏览器中验证 common 可编辑、admin 只读、筛选与保存交互，并检查控制台和失败请求。
- [x] 3.3 执行差异检查和只读代码评审，关闭阻断问题。
