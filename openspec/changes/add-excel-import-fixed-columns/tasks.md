# 实施任务清单

## 1. Proposal 阶段（必须先完成并获批）
- [x] 1.1 明确 Excel 支持范围（只支持 `.xlsx`）与文件大小上限
- [x] 1.2 明确列号基准（1-based）与 UI 表达（数字/字母）
- [x] 1.4 明确合并单元格展开（表头+数据区）、空行结束（已用区域末尾）、公式策略（不考虑）

## 4. 验证
- [x] 4.1 `dotnet build` 通过
- [x] 4.2 `pnpm build` 通过（如适用）
- [ ] 4.3 端到端手工验证：包含合并单元格与跨行表头的样例 Excel
