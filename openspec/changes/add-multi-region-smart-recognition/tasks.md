## 1. Specification Gate
- [x] 1.1 严格校验 `add-multi-region-smart-recognition`。
- [x] 1.2 获得提案批准后再修改业务源码。

## 2. Region Contracts and Persistence
- [x] 2.1 新增识别、确认、导入和智能填充共享的区域契约及组合身份。
- [x] 2.2 新增 `DocumentTemplateRegion` 实体、EF 配置、仓储读取和兼容迁移。
- [x] 2.3 扩展模板保存与命中逻辑，旧模板回退为单区域。

## 3. Recognition and Learning
- [x] 3.1 实现 Excel 重复表头块与连续数据区域检测。
- [x] 3.2 扩展逐区域确定性健康检查、合并列冲突检查和可解释问题。
- [x] 3.3 扩展确认用例，按最终区域保存模板并幂等学习未录入列映射。

## 4. Import and Smart Fill Execution
- [x] 4.1 数据导入预览与执行支持同一 Sheet 多区域，并保持旧请求兼容。
- [x] 4.2 智能填充预览、执行、写回和审批令牌支持区域结束行与组合身份。
- [x] 4.3 移除验收列缺失时回退规格列的行为，并提供明确阻断原因。
- [x] 4.4 扩展执行历史与错误/跳过行归属，避免同 Sheet 多区域覆盖。

## 5. Frontend Experience
- [x] 5.1 新增共享区域模型、A1 范围格式化与组合键工具。
- [x] 5.2 将导入和智能填充确认卡改为区域摘要、范围标签和忽略行统计。
- [x] 5.3 新增“调整范围”抽屉，并默认隐藏原始数字和列配置。
- [x] 5.4 将确认动作接入结构模板和缺失列映射学习，展示模板命中/已学习状态。

## 6. Verification
- [x] 6.1 后端单元与集成测试覆盖单区域兼容、多区域识别、范围越界/重叠、合并列冲突和确认学习去重。
- [x] 6.2 前端测试覆盖 A1 范围展示、区域选择/编辑、组合键和导入/填充请求组装。
- [x] 6.3 使用 `翻板机验收规范-SAC20260514.xlsx` 验证识别两区共 120 行，忽略中间与尾部非规格内容。
- [x] 6.4 运行 `dotnet test AcceptanceSpecSystem.sln -c Debug`、前端测试、类型检查和构建。

## 7. Review Hardening
- [x] 7.1 识别与确认上下文同时绑定文件和客户，并在后端校验客户数据权限。
- [x] 7.2 无可信后续区域时保留原单区域范围，所有采用区域执行独立健康检查。
- [x] 7.3 使用稳定区域行身份保存剔除选择，覆盖部分预览切换完整预览。
- [x] 7.4 服务端校验匹配区域并从最终区域配置推导写回目标列，拒绝陈旧或篡改快照。
- [x] 7.5 旧模板负数列归一化，模板身份覆盖多区域结构变体且支持安全长度指纹。
- [x] 7.6 模板保存与列规则学习具备事务和并发幂等语义，并逐区域学习。
- [x] 7.7 待补列 Sheet 可进入确认页，确认卡使用完整 TableInfo 并刷新每个区域表头。
- [x] 7.8 高级设置使用真实行号、共享范围校验，开放结束行计数与修改判定一致。
- [x] 7.9 重新识别和切换客户清空旧配置，多 Sheet 确认不会产生前后端状态分叉。
- [x] 7.10 Word 多区域严格限制闭区间，导入/填充请求保持旧单区域兼容。
- [x] 7.11 导入和智能填充重试具备稳定幂等语义，checkpoint 包含全部行为配置。
- [x] 7.12 执行历史、错误/跳过行号和多区域表头展示保留区域归属及统一坐标口径。
- [x] 7.13 补齐跨客户、空白带、旧模板、结构变体、预览扩容、篡改快照、Word 区域、并发与重试反例测试。
- [x] 7.14 模板确认使用跨实例操作锁和唯一冲突回读；旧模板对移列新增区域降级确认；全局学习词以数据库唯一键阻断跨字段冲突，并补齐规范化迁移与并发回归测试。
- [x] 7.15 修复前端重新识别、应用到其他 Sheet、抽屉取消与旧枚举兼容的状态同步问题，并补齐范围编辑和拒绝原因的无障碍提示。
- [x] 7.16 导入执行使用租户作用域操作锁、短事务、幂等任务过期清理和受控源文件清理，覆盖并发重复导入与失败恢复。
- [x] 7.17 智能填充使用租户作用域任务身份、跨实例操作锁和持久化文件恢复日志；写回保留 Excel 结构化表并拒绝合并单元格重复目标，覆盖提交结果不确定时的恢复路径。
- [x] 7.18 Excel 区域调整简化为直接编辑 A1 单列范围，并反算共享行列配置；导入和智能填充共用格式、边界、一致性与重叠校验，Word 保持原配置方式。
- [x] 7.19 已确认且结构稳定的多区域客户模板允许自动采用；首次发现、模板漏区、结构变化和区域健康检查异常继续降级确认，并补齐回归测试。
- [x] 7.20 智能确认卡合并展示表级与区域级识别问题，明确旧模板漏区等首次确认原因并去重。
- [x] 7.21 Excel 确认卡移除重复高级行列配置，以 A1 范围作为唯一编辑源；按数据起始行向上定位最近有效单行表头并展开合并单元格，项目范围留空时反推仅规格模式。
