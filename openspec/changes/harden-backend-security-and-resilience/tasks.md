## 0. 批准

- [x] 0.1 评审并批准本提案；批准前不修改生产代码。

## 1. 管理员与身份安全

- [x] 1.1 为管理员变更增加角色区间校验和公司级跨实例操作锁。
- [x] 1.2 覆盖过期、未来、形成空档以及并发降级/删除管理员场景。
- [x] 1.3 将 Application 契约、服务和前端表单的新建/重置密码最低长度提升到 12，同时保持已有账号登录兼容。

## 2. 预览进度归属

- [x] 2.1 为全部进度跟踪操作增加公司/用户归属和请求标识校验。
- [x] 2.2 跨 owner 查询返回 404，拒绝运行中重复标识并脱敏进度失败信息。
- [x] 2.3 增加同标识跨用户/跨公司隔离和错误脱敏 API 回归测试。

## 3. 取消、日志与备份

- [x] 3.1 移除 Prompt 和业务正文日志，并增加日志捕获回归测试。
- [x] 3.2 在高价值 Application、Repository、EF 和事务路径中贯穿 CancellationToken。
- [x] 3.3 以 partial 文件生成数据库备份，取消时终止进程树、清理临时输出并原子发布成功产物。
- [x] 3.4 在不依赖真实 mysqldump 的情况下增加取消、非零退出和清理故障注入测试。

## 4. 有界批量上传

- [x] 4.1 在 API、代理和 Application 之间定义一致的文件数、单文件和整批请求预算。
- [x] 4.2 用顺序流式临时存储替代整批字节数组物化。
- [x] 4.3 为上传和旧预览入口增加取消与部分失败补偿测试。

## 5. 验证

- [x] 5.1 实施期间运行后端、前端契约和安全定向回归测试。
- [x] 5.2 全部阶段收口后只运行一次完整后端/前端测试、生产构建和 OpenSpec 严格校验。
