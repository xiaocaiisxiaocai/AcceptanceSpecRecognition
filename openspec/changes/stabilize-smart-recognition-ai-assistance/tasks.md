## 1. 契约与回归基线

- [x] 1.1 增加 readiness 显式服务状态矩阵失败测试。
- [x] 1.2 增加 Ollama JSON Schema 序列化和非法输出修正失败测试。
- [x] 1.3 增加智能结构识别 `aiAssist` 响应状态失败测试。
- [x] 1.4 增加前端 checking、取消和降级提示失败测试。

## 2. 后端实现

- [x] 2.1 区分自动选择与显式服务的 readiness 执行语义。
- [x] 2.2 为结构化 LLM 场景传递 Schema 并映射到 Ollama `format`。
- [x] 2.3 实现格式错误的一次有预算修正并保留严格业务校验。
- [x] 2.4 聚合并返回智能结构识别 AI 辅助执行摘要。
- [x] 2.5 补充脱敏日志和低基数结果指标。

## 3. 前端实现

- [x] 3.1 让 checking 等待窗口覆盖后端探测上限并继续支持取消。
- [x] 3.2 解析 `aiAssist` 并明确展示 AI 已应用、部分应用或规则回退。
- [x] 3.3 保持数据导入与智能填充共享链路行为一致。

## 4. 验证与交付

- [x] 4.1 运行相关 Core、Application、API 和前端定向测试。
- [x] 4.2 运行后端完整测试、前端类型检查、构建和代码规范检查。
- [x] 4.3 执行真实重复识别性能与降级一致性验证。
- [x] 4.4 完成分层代码评审并关闭 Critical/Important 问题。
- [x] 4.5 更新任务状态并运行 `openspec validate stabilize-smart-recognition-ai-assistance --strict`。

> 说明：完整后端测试已执行；其中 API 旧上传夹具和契约快照存在可在原始 `main` 复现的既有失败，本变更相关 API 测试均已通过。
