## 1. 数据模型与迁移

- [x] 1.1 为验收规格增加引用内容版本，并新增逐次引用历史实体。
- [x] 1.2 配置级联外键、任务幂等唯一索引和按规格/版本/时间分页索引。
- [x] 1.3 生成可回滚的 EF Core Migration，将已有非零引用次数写为时间不可追溯的版本 1 基线。
- [x] 1.4 登记安全迁移并验证迁移元数据、模型快照和基线合计不变量。

## 2. 引用历史写入与内容版本

- [x] 2.1 扩展统一内容变化策略，在实质变化时递增引用版本、清零当前计数并保留旧历史。
- [x] 2.2 在 Excel 与 Word 最终提交事务内按每个最终采用行写入引用时间、任务标识和任务内序号。
- [x] 2.3 使用 `TimeProvider` 记录 UTC 成功提交时间，并用版本条件原子更新当前引用次数。
- [ ] 2.4 覆盖批量采用、并发累计、内容并发修改、幂等重放、失败/取消回滚和空内容不计数测试。
- [ ] 2.5 覆盖手工编辑、文档导入、智能填充回填和备注批量替换的统一版本递增规则。

## 3. API 与权限

- [x] 3.1 增加引用历史仓储查询、应用模型和 API 契约，支持最早/最新稳定分页并返回当前版本汇总。
- [x] 3.2 新增 `GET /api/specs/{id}/reference-history`，支持选择是否包含历史版本。
- [ ] 3.3 复用验收规格读取权限和数据范围，覆盖允许、越权、缺失规格和分页边界测试。
- [x] 3.4 验证当前版本引用次数等于当前版本历史 `OccurrenceCount` 合计，且迁移基线不伪造时间。

## 4. 前端交互

- [x] 4.1 扩展规格 API 类型与引用历史请求封装。
- [x] 4.2 将列表和详情中的引用次数改为可点击入口；从未引用的初始版本保持只读，当前零次但有旧版本时允许查看旧历史。
- [x] 4.3 实现引用历史抽屉，展示汇总、当前/全部版本切换、最早/最新排序、引用序号、引用时间和服务端分页。
- [x] 4.4 对迁移前未知时间显示明确提示，并复用现有 UTC 到本地时间格式化逻辑。
- [x] 4.5 增加前端定向测试，覆盖入口、筛选、分页、未知时间和请求失败状态。

## 5. 验证

- [x] 5.1 运行引用历史相关 API 集成测试、数据迁移测试和受影响的引用次数回归测试。
- [x] 5.2 运行前端定向测试、类型检查和受影响模块构建。
- [x] 5.3 运行 `openspec validate add-acceptance-spec-reference-history --strict` 并检查 Git 差异范围。
- [ ] 5.4 在本地浏览器验证列表与详情入口、可见内容、控制台错误和失败请求；记录未覆盖边界。
