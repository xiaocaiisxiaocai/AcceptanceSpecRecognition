## 1. 数据模型与迁移

- [x] 1.1 为内容版本快照建立实体、仓储和 EF 映射，配置唯一索引、分页索引、删除外键及当前版本并发令牌。
- [x] 1.2 生成可回滚迁移，为所有现有规格插入当前正文基线，不伪造缺失旧版本。
- [x] 1.3 登记迁移安全性并验证 MySQL/SQLite 模型、索引、基线版本和历史缺口标识。

## 2. 统一版本写入

- [x] 2.1 先用失败测试定义内容等价、实质变化、版本递增、引用次数清零和快照不变量。
- [x] 2.2 实现统一版本协调器，替代各入口直接调用现有静态计数策略。
- [x] 2.3 接入手工新增、批量导入、Word/Excel 文档导入和智能填充回填创建的 V1 快照。
- [x] 2.4 接入手工编辑、文档导入覆盖、智能填充覆盖/部分更新和部门备注批量替换的新版本快照。
- [x] 2.5 验证实体、快照、Embedding 缓存失效和调用方写入同事务提交，失败/取消/并发冲突不留下单边状态。
- [x] 2.6 完成 `add-acceptance-spec-reference-history` 中 2.4、2.5 的相关入口与并发测试，不直接改写未验证任务状态。

## 3. API、权限与恢复

- [x] 3.1 增加版本列表、版本详情和字段级差异的应用模型及只读 API。
- [x] 3.2 为更新请求增加可选期望版本和修改原因，前端正式调用必须发送当前版本。
- [x] 3.3 实现恢复旧版本为新版本的事务用例、缓存清理、冲突和空恢复拒绝。
- [x] 3.4 增加 `restore-version` API/按钮权限种子，默认仅管理员，并接入安全审计详情。
- [x] 3.5 覆盖允许、越权、跨组织、缺失规格、缺失版本、分页边界和恢复并发冲突。
- [x] 3.6 完成 `add-acceptance-spec-reference-history` 中 3.3 的读取权限与分页边界测试，不直接改写未验证任务状态。

## 4. 前端版本工作台

- [x] 4.1 扩展版本列表、详情、差异、恢复和期望版本请求类型。
- [x] 4.2 将列表和详情的版本标签改为版本历史入口，并保持引用历史入口语义独立。
- [x] 4.3 实现紧凑版本工作台，展示历史完整性、来源、操作者、时间和完整正文。
- [x] 4.4 实现任意两个可用版本的字段级对比，安全显示长文本和空值变化。
- [x] 4.5 实现有权限的恢复确认，明确将创建的新版本，并处理 409 冲突后刷新。
- [x] 4.6 完成桌面和移动布局、加载/空/错误/无权状态及无重叠验收。

## 5. 验证与交付

- [x] 5.1 运行内容版本、引用统计、文档导入、智能填充回填、备注替换和权限相关定向后端测试。
- [x] 5.2 运行前端定向测试、类型检查、ESLint、Prettier、Stylelint 和受影响构建。
- [x] 5.3 运行迁移列表与模型校验，验证当前版本恰有一个快照、版本唯一和缺失旧正文不被伪造。
- [x] 5.4 使用真实浏览器验证列表、详情、时间线、差异、恢复、并发冲突、权限、控制台错误和失败请求。
- [x] 5.5 执行差异审查与 `openspec validate add-acceptance-spec-content-version-history --strict`，关闭 Critical/Important 问题。
