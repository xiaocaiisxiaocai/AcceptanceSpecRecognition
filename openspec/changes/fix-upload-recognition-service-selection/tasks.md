## 1. Specification Gate
- [x] 1.1 严格校验 `fix-upload-recognition-service-selection`。
- [x] 1.2 获得提案批准后再修改业务源码。

## 2. Upload and Metadata State
- [x] 2.1 为上传、表结构读取、就绪和读取失败建立独立页面状态。
- [x] 2.2 数据导入与智能填充复用按文件标识去重的表结构加载结果，移除隐藏组件的重复请求。
- [x] 2.3 表结构就绪前阻止智能识别，并增加重试和操作级超时。

## 3. Explicit Recognition Service Selection
- [x] 3.1 扩展智能结构识别请求契约，未显式开启时不调用 LLM。
- [x] 3.2 两处页面新增 AI 增强开关；有可用服务时默认开启并在内部采用优先级最高项，不展示服务下拉，无可用服务时禁用并提示配置。
- [x] 3.3 后端关闭时跳过 LLM，开启时只调用所选的已启用 LLM。
- [x] 3.4 所选服务认证、连接或超时失败后对本次文档熔断，保留规则识别结果。

## 4. Service Health
- [x] 4.1 完整测试所有当前已启用的 LLM 与 Embedding 配置。
- [x] 4.2 禁用确认不可用的配置并复查启用列表。

## 5. Verification
- [x] 5.1 后端测试覆盖默认零 LLM 调用、严格服务标识、无跨服务回退和请求级熔断。
- [x] 5.2 前端测试覆盖上传阶段、元数据重试、请求去重、AI 默认采用、无服务配置入口、按钮门禁和两处请求组装。
- [x] 5.3 运行后端测试、前端测试、类型检查和生产构建。
- [x] 5.4 浏览器复测 Excel、Word 和多表文件的上传、表结构读取及规则/AI 两种识别路径。
- [x] 5.5 浏览器复测两个入口在存在可用 LLM 时默认开启、展示自动采用的服务且不显示选择下拉。
