## 1. Backend RED
- [x] 1.1 为 Word、Excel 成功执行、失败补偿、幂等重试和旧记录降级增加结果存档测试。
- [x] 1.2 为普通用户本部门范围、空部门归属回退、管理员公司范围、部门/操作人筛选、跨公司隔离、下载命名空间校验、文件缺失和内容完整性增加 API 测试。

## 2. Persistence And Storage GREEN
- [x] 2.1 为 `ExecutionHistoryRecord` 增加可空的结果存档元数据并创建 EF Core 迁移。
- [x] 2.2 扩展文件存储服务，使用独立命名空间保存智能填充最终结果文件并校验安全路径。
- [x] 2.3 在 Word 与 Excel 最终提交链路中原子编排执行记录、结果存档、失败补偿和幂等复用。
- [x] 2.4 将结果存档引用纳入孤儿文件巡检与执行历史保留清理验证。

## 3. Query And Download API GREEN
- [x] 3.1 扩展执行历史列表契约，支持填充存档所需的时间、部门、操作人筛选和结构化存档摘要，并按普通用户/管理员身份应用部门或公司范围。
- [x] 3.2 增加按执行记录 ID 下载结果存档的只读接口，并执行普通用户本部门范围、空归属回退、管理员公司范围、类型、路径和文件存在性校验。
- [x] 3.3 保持现有执行历史详情、逐行回放和 24 小时任务下载接口兼容。

## 4. Frontend GREEN
- [x] 4.1 在导航与权限清单中增加“其他 > 填充存档”入口。
- [x] 4.2 实现紧凑列表、来源文件、时间、部门和操作人筛选、存档状态、当前页摘要及下载操作，不使用抽屉。
- [x] 4.3 对旧记录无存档、文件缺失、无权限、加载失败和下载失败提供明确状态。
- [x] 4.4 增加前端请求、筛选状态和页面交互测试。

## 5. Verification And Review
- [x] 5.1 运行相关后端测试、迁移验证和架构测试。
- [x] 5.2 运行前端定向测试、类型检查、代码规范检查和生产构建。
- [x] 5.3 在真实浏览器分别验证同部门普通用户、不同部门普通用户和管理员的菜单、数据范围、筛选、详情、Word/Excel 下载、窄屏布局、控制台错误和失败请求。
- [x] 5.4 执行 `openspec validate add-smart-fill-result-archives --strict`、`git diff --check` 并完成分层自审。
