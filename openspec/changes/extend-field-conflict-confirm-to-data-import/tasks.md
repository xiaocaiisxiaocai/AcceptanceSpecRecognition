## 0. 批准

- [x] 0.1 用户审阅并明确批准将字段候选冲突弹框扩展到数据导入。

## 1. 共享能力

- [x] 1.1 将字段冲突收集、选择应用逻辑抽取为数据导入与智能填充可复用的共享模块。
- [x] 1.2 将冲突选择弹框调整为中性共享组件，并保持智能填充现有交互不变。

## 2. 数据导入接入

- [x] 2.1 在数据导入文件级主操作前收集已选 Sheet 的未解决字段冲突。
- [x] 2.2 用户确认后更新识别结果和确认草稿，并继续原批量确认学习与导入流程。
- [x] 2.3 用户取消时停留在识别确认步骤并保留现有页面状态。

## 3. 验证

- [x] 3.1 覆盖无冲突直达、单/多 Sheet 冲突、确认继续、取消保留和未选 Sheet 跳过场景。
- [x] 3.2 运行数据导入、智能填充共享冲突逻辑的定向测试、类型检查、代码规范检查和 OpenSpec 严格校验。

## 4. 冲突确认前置

- [x] 4.0 用户已确认字段冲突应在结构候选识别后、正式预览和匹配前处理。
- [x] 4.1 结构识别后立即拦截默认已选 Sheet 的冲突，确认前不加载导入预览。
- [x] 4.2 用户确认后按最终列构建结构草稿和导入预览，取消时停留在结构确认步骤。
- [x] 4.3 保留后续勾选冲突 Sheet 的文件级兜底门禁。
- [x] 4.4 覆盖前置确认、取消不加载预览、无冲突直达和后续勾选兜底场景。
