## 1. 后端组织移动契约

- [x] 1.1 先补充成功移动完整子树、幂等移动和拒绝根节点/跨公司/自身/后代/停用或类型不合法目标的失败集成测试，并观察 RED。
- [x] 1.2 增加移动请求契约、应用服务方法和独立移动端点，使用单事务更新 `ParentId`、`Path`、`Depth` 与 `UpdatedAt`。
- [x] 1.3 增加 `api:org-unit:move`、`btn:org-unit:move` 权限种子与审计契约测试。
- [x] 1.4 验证移动后用户组织关系、角色范围节点、验收规格和文件归属 ID 保持不变，失败路径不产生部分更新。

## 2. 前端移动交互

- [x] 2.1 先补充移动 API 和组织页面交互契约测试，并观察 RED。
- [x] 2.2 在组织表格增加受 `btn:org-unit:move` 控制的“移动”操作，根节点不显示入口。
- [x] 2.3 增加目标上级选择弹窗，过滤自身、后代、当前上级、停用和类型不合法节点，并显示受影响节点数量及范围变化提示。
- [x] 2.4 移动成功后刷新组织树，失败时保留弹窗选择并显示服务端具体原因。

## 3. 验证与评审

- [x] 3.1 运行组织、权限、数据范围和应用边界相关后端定向测试。
- [x] 3.2 运行前端定向测试、类型检查和受影响文件格式/规范检查。
- [x] 3.3 启动本地项目，以管理员真实执行“新增事业部 → 移动部门 → 核对子树 → 移回原上级”的浏览器 E2E。
- [x] 3.4 只读复核事务、循环防护、权限、审计、缓存失效、引用稳定性和工作区边界，关闭阻断问题。
