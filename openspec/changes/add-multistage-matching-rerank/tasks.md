## 1. 设计与模型

- [ ] 1.1 扩展匹配配置模型，增加策略、召回数量、重排模式、歧义判定相关参数，并保持默认值兼容现有单阶段行为
- [ ] 1.2 明确多阶段模式下的阈值语义、歧义判定公式与预览结果结构，包括策略信息、歧义状态、重排得分明细与最终选中原因

## 2. 后端实现

- [ ] 2.1 在 Core 层实现 `TopK` 召回能力，而不是仅保留当前最终 `Top1` 结果
- [ ] 2.2 实现基于项目一致性、数值/单位、关键词重合、冲突词等结构化信号的规则重排
- [ ] 2.3 实现“仅对高歧义候选触发 LLM 复核”的按需执行逻辑
- [ ] 2.4 更新预览与批量预览接口，使其支持多阶段参数与结果输出
- [ ] 2.5 更新执行与批量执行填充接口，确保其使用预览确认结果，不在执行阶段静默重算不同的默认匹配

## 3. 前端实现

- [ ] 3.1 在智能填充配置界面中增加匹配策略切换与高级参数设置
- [ ] 3.2 在预览界面中展示当前策略、歧义状态和必要的重排说明

## 4. 验证与文档

- [ ] 4.1 补充单阶段兼容性测试，确保不传新参数时行为与当前版本一致
- [ ] 4.2 补充多阶段召回、阈值过滤、歧义判定、规则重排、歧义触发 LLM 与预览/执行一致性的单元测试与集成测试
- [ ] 4.3 基于真实样本文档建立首批准确率评估基线并记录结果
- [ ] 4.4 更新用户文档与开发文档，说明何时使用单阶段或多阶段策略
